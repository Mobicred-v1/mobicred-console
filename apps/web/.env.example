# Explicit design preview. Never set CONSOLE_ENV=preview on a production deployment.
CONSOLE_ENV=development
CONSOLE_PREVIEW_ENABLED=false
# Server-only BFF and staff authentication settings are documented in docs/security-foundation.md.
CONSOLE_API_URL=http://localhost:3005
